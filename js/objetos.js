/*

const persona = {
  nombre: "CH",
  edad: 8,
  domicilio: "Palermo, B.A.",
};

function Persona(nombre, edad, domicilio) {
  this.nombre = nombre;
  this.edad = edad;
  this.domicilio = domicilio;
  /*this.miComidaPreferida = function (cp) {
    console.log("Mi comida preferida es " + cp);
  }
  this.salidaPreferida = (sp) => {
    console.log("Mi salida preferida es: " + sp);
  };
}

const padre = new Persona("Homero", 39, "Siempreviva123");
const madre = new Persona("Marge", 34, "Siempreviva123");
const hijo = new Persona("Bart", 10, "Siempreviva123");

function listaEstructuraDeObj() {
  for (propiedad in padre) console.log(propiedad);
}
*/
