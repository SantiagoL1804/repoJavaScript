//Sin parametros
/*function solicitarNombre() {
  let nombreIngresado = prompt("Ingresar nombre");
  alert("El nombre ingresado es " + nombreIngresado);
}

//Con parametros

function calcular(param1, param2) {
  console.log(parseFloat(param1) * parseFloat(param2));
}

param1 = prompt("Ingrese el primer nro.");
param2 = prompt("Ingrese el segundo nro.");

calcular(param1, param2);
*/
/*
function calculadora() {
  debugger;
  let primerNro = parseFloat(prompt("Ingrese el primer número:"));
  let segundoNro = parseFloat(prompt("Ingrese el segundo número:"));
  let operador = prompt("Ingrese el tipo de operación a realizar: ( + - * /)");
  console.log(obtenerCalculo(primerNro, segundoNro, operador));
}

function obtenerCalculo(primerNro, segundoNro, operador) {
  switch (operador) {
    case "+":
      return primerNro + segundoNro;
    case "-":
      return primerNro - segundoNro;
    case "*":
      return primerNro * segundoNro;
    case "/":
      return primerNro / segundoNro;
  }
}

let multiplicacion = function (a, b) {
  return a * b;
};

const sumarflecha = (a, b) => a + b;
*/
